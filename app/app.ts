// ⚠️ AUTO-GENERATED bởi `fx sync` (quét app/cells/*). ĐỪNG sửa tay.
// Thêm trang = tạo app/cells/<id>/index.tsx rồi chạy `fx sync` (dev/resolve tự gọi).
import type { CellDef } from "../src/core/engine";
import c_admin from "./cells/admin/index";
import c_greet from "./cells/greet/index";
import c_hello from "./cells/hello/index";
import c_home from "./cells/home/index";
import c_secret from "./cells/secret/index";
import c_slow from "./cells/slow/index";
import c_todos from "./cells/todos/index";

export const cells: CellDef<any, any>[] = [c_admin, c_greet, c_hello, c_home, c_secret, c_slow, c_todos];
